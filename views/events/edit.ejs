<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | EventHub</title>
  <link rel="stylesheet" href="/styles/style-app.css">
  <link rel="icon" href="/images/icon.png" type="image/png">
  <link href="https://fonts.googleapis.com/css2?family=Jost:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="navbar">
    <div class="nav-container container">
      <div class="logo">
        <span><a href="http://localhost:3000"><img src="/images/icon.png" alt="Иконка EventHub" width="40" height="40"></a></span>
        <span>EventHub</span>
      </div>
      <div class="nav-links">
          <a href="/app" class="nav-link">Главная</a>
          <a href="/events" class="nav-link">Мои мероприятия</a>
          <a href="/auth/logout" class="nav-link">Выйти (<%= user.username %>)</a>
      </div>
    </div>
  </nav>

  <div class="container">
    <h1><%= title %></h1>
    
    <% if (messages.error) { %>
      <div class="alert alert-error"><%= messages.error %></div>
    <% } %>

    <form action="/events/<%= event._id %>?_method=PUT" method="POST" class="event-form">
      <div class="form-group">
        <label for="title">Название</label>
        <input type="text" id="title" name="title" value="<%= event.title %>" required>
      </div>

      <div class="form-group">
        <label for="description">Описание</label>
        <textarea id="description" name="description" required><%= event.description %></textarea>
      </div>

      <div class="form-group">
        <label for="format">Формат мероприятия</label>
        <select id="format" name="format" class="form-control" required>
          <option value="online" <%= event.format === 'online' ? 'selected' : '' %>>Онлайн</option>
          <option value="offline" <%= event.format === 'offline' ? 'selected' : '' %>>Оффлайн</option>
        </select>
      </div>
    
      <div class="form-group" id="onlineFields" <%= event.format === 'offline' ? 'style="display:none"' : '' %>>
        <label for="onlineLink">Ссылка на онлайн-мероприятие</label>
        <input type="url" id="onlineLink" name="onlineLink" value="<%= event.onlineLink || '' %>">
      </div>
    
      <div class="form-group" id="offlineFields" <%= event.format === 'online' ? 'style="display:none"' : '' %>>
        <label for="location">Место проведения</label>
        <input type="text" id="location" name="location" value="<%= event.location || '' %>">
      </div>
    
      <div class="form-group">
        <label for="maxParticipants">Максимальное количество участников</label>
        <input type="number" id="maxParticipants" name="maxParticipants" 
               value="<%= event.maxParticipants || '' %>" min="0">
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="startDate">Дата начала</label>
          <input type="date" id="startDate" name="startDate" 
                 value="<%= event.startDate ? event.startDate.toISOString().split('T')[0] : '' %>" required>
        </div>
        <div class="form-group">
          <label for="startTime">Время начала</label>
          <input type="time" id="startTime" name="startTime" 
                 value="<%= event.startTime || '' %>" required>
        </div>
      </div>
      
      <div class="form-row">
        <div class="form-group">
          <label for="endDate">Дата окончания</label>
          <input type="date" id="endDate" name="endDate" 
                 value="<%= event.endDate ? event.endDate.toISOString().split('T')[0] : '' %>" required>
        </div>
        <div class="form-group">
          <label for="endTime">Время окончания</label>
          <input type="time" id="endTime" name="endTime" 
                 value="<%= event.endTime || '' %>" required>
        </div>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn btn-primary">Сохранить</button>
        <a href="/events" class="btn btn-outline">Отмена</a>
      </div>
    </form>
  </div>

  <footer class="footer">
    <div class="container text-center py-4">
      <p>© 2025 EventHub.</p>
    </div>
  </footer>

  <script>
    document.getElementById('format').addEventListener('change', function() {
      const format = this.value;
      document.getElementById('onlineFields').style.display = format === 'online' ? 'block' : 'none';
      document.getElementById('offlineFields').style.display = format === 'offline' ? 'block' : 'none';
    });
  </script>
</body>
</html>