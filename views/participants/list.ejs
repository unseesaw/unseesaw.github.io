<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles/style-app.css">
</head>
<body>
  <nav class="navbar">
    <div class="nav-container container">
      <span><a href="http://localhost:3000"><img src="/images/icon.png" alt="Иконка EventHub" width="40" height="40"></a></span>
      <div class="nav-links">
        <a href="/app" class="nav-link">Главная</a>
        <a href="/events" class="nav-link">Мероприятия</a>
        <a href="/calendar" class="nav-link">Календарь</a>
        <a href="/auth/logout" class="nav-link">Выйти (<%= user.username %>)</a>
      </div>
    </div>
  </nav>
  <div class="container">
    <h1>Участники мероприятия: <%= event.title %></h1>
        
    <% if (participants.length === 0) { %>
      <p>Участников нет</p>
    <% } else { %>
      <table class="participants-table">
        <thead>
          <tr>
            <th>Имя</th>
            <th>Телефон</th>
            <th>Дата регистрации</th>
          </tr>
        </thead>
        <tbody>
          <% participants.forEach(participant => { %>
            <tr>
              <td><%= participant.name %></td>
              <td><%= participant.phone %></td>
              <td><%= participant.registeredAt.toLocaleString() %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } %>

    <a href="/events/<%= event._id %>" class="btn btn-outline">Назад к мероприятию</a>
  </div>

  <footer class="footer">
    <div class="container text-center py-4">
      <p>© 2025 EventHub.</p>
    </div>
  </footer>
</body>
</html>